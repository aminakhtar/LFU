<Window x:Class="LFU.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preview" 
        Height="300" Width="600"
        WindowStyle="ToolWindow"
        Topmost="True"
        ShowInTaskbar="False"
        >

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Row="0" Grid.Column="0" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible">
            <TextBlock x:Name="tblPreview"  TextWrapping="NoWrap" Margin="10,10,10,10"/>
        </ScrollViewer>

    </Grid>

</Window>
